# Workflow agent

This repository contains attempts at creating a workflow agent. The purpose of the agent is to assemble a function based on available tools for a given prompt, where agent would not code anything directly but instead returned workflow that consists of available tools connected though wiring their input/output models. 